# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# OC-SORT tracker settings for Ultralytics mode="track"
# Minimal config aligned with ByteTrack keys for compatibility

tracker_type: ocsort   # tracker type, choices=[ocsort]

# 常用阈值（可按场景微调）
track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.25
match_thresh: 0.8
track_buffer: 30
fuse_score: True

# 其他可选参数
with_reid: False
gmc_method: none
proximity_thresh: 0.5
appearance_thresh: 0.25